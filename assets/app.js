const root=document.documentElement,btn=document.querySelector('#themeToggle');const saved=localStorage.getItem('theme');if(saved)root.dataset.theme=saved;btn?.addEventListener('click',()=>{const t=root.dataset.theme==='light'?'dark':'light';root.dataset.theme=t;localStorage.setItem('theme',t);btn.textContent=t==='light'?'☀︎':'☾'});
const render=()=>{if(window.renderMathInElement)renderMathInElement(document.body,{delimiters:[{left:'$$',right:'$$',display:true},{left:'\\[',right:'\\]',display:true},{left:'\\(',right:'\\)',display:false}],throwOnError:false})};window.addEventListener('load',render);
const updateProgress=()=>{const h=document.documentElement;const max=h.scrollHeight-h.clientHeight;h.style.setProperty('--progress',max?`${100*h.scrollTop/max}%`:'0%')};addEventListener('scroll',updateProgress,{passive:true});updateProgress();
if(matchMedia('(pointer:fine)').matches&&!matchMedia('(prefers-reduced-motion:reduce)').matches){document.querySelectorAll('.qcard,.day').forEach(card=>{card.addEventListener('pointermove',e=>{const r=card.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;card.style.transform=`perspective(900px) rotateX(${-y*5}deg) rotateY(${x*7}deg) translateY(-2px)`});card.addEventListener('pointerleave',()=>card.style.transform='')})}
const nInput=document.querySelector('#simN'),kInput=document.querySelector('#simK'),lights=document.querySelector('#lights'),record=document.querySelector('#record');let state=[];
function normalize(){let n=Math.max(3,Math.min(30,+nInput.value||8)),k=Math.max(2,Math.min(n-1,+kInput.value||3));nInput.value=n;kInput.value=k;if(state.length!==n)state=Array.from({length:n},(_,i)=>state[i]??(i%3===0?1:0));return[n,k]}
function drawSim(){if(!lights)return;const[n,k]=normalize();lights.innerHTML='';state.forEach((v,i)=>{const b=document.createElement('button');b.className='light '+(v?'on':'');b.textContent=i+1;b.title=`Đèn ${i+1}: ${v?'sáng':'tắt'}`;b.onclick=()=>{state[i]^=1;drawSim()};lights.append(b)});const s=state.map((_,i)=>Array.from({length:k},(_,j)=>state[(i+j)%n]).reduce((a,b)=>a+b,0));record.textContent=`(${s.join(', ')}) · gcd(${n},${k})=${gcd(n,k)}`}
function gcd(a,b){while(b)[a,b]=[b,a%b];return a}document.querySelector('#simRun')?.addEventListener('click',drawSim);document.querySelector('#simRandom')?.addEventListener('click',()=>{const[n]=normalize();state=Array.from({length:n},()=>Math.random()<.5?0:1);drawSim()});nInput?.addEventListener('change',drawSim);kInput?.addEventListener('change',drawSim);drawSim();